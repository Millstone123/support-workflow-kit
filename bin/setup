#!/bin/sh
set -eu

SUPPORT_EDITOR=${SUPPORT_EDITOR:-}
editor=$SUPPORT_EDITOR
./bin/status >/dev/null
if [ -n "$editor" ] && command -v "$editor" >/dev/null 2>&1; then
  "$editor" .support/status.txt >/dev/null 2>&1 &
fi
cat .support/status.txt
