#!/bin/sh
set -eu

workspace=$(sed -n 's/.*"workspace"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p' config/workspace.json | head -1)
case "$workspace" in
  ''|*[!A-Za-z0-9._-]*) echo 'Invalid workspace name' >&2; exit 2 ;;
esac
if [ ! -f .support/status.txt ]; then
  mkdir -p .support
  printf 'workspace=%s\nstatus=ready\n' "$workspace" > .support/status.txt
fi
cat .support/status.txt
